import {NextRequest,NextResponse} from 'next/server';
export async function POST(req:NextRequest){const base=process.env.VESTA_LEARNING_API_URL?.replace(/\/$/,'');if(!base)return NextResponse.json({ok:false,error:'Learning service is not configured.'},{status:503});const body=await req.json();try{const r=await fetch(`${base}/v1/score`,{method:'POST',headers:{'content-type':'application/json',...(process.env.VESTA_LEARNING_API_KEY?{authorization:`Bearer ${process.env.VESTA_LEARNING_API_KEY}`}:{})},body:JSON.stringify(body),cache:'no-store'});return NextResponse.json(await r.json(),{status:r.status})}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'Learning service unavailable'},{status:502})}}
