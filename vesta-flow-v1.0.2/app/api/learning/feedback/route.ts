import {NextRequest,NextResponse} from 'next/server';
import {learningHeaders,learningResponse} from '../proxy';
export async function POST(req:NextRequest){const url=process.env.VESTA_LEARNING_API_URL?.replace(/\/$/,'');if(!url)return NextResponse.json({ok:false,error:'Learning API not configured'},{status:503});let body:unknown;try{body=await req.json()}catch{return NextResponse.json({ok:false,error:'Request body must be valid JSON.'},{status:400})}try{const r=await fetch(`${url}/v1/feedback`,{method:'POST',headers:learningHeaders(true),body:JSON.stringify(body),cache:'no-store',signal:AbortSignal.timeout(8_000)});return learningResponse(r)}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'Learning service unavailable'},{status:502})}}
