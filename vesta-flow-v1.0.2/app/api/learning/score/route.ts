import {NextRequest,NextResponse} from 'next/server';
import {learningHeaders,learningResponse} from '../proxy';
export async function POST(req:NextRequest){const base=process.env.VESTA_LEARNING_API_URL?.replace(/\/$/,'');if(!base)return NextResponse.json({ok:false,error:'Learning service is not configured.'},{status:503});let body:unknown;try{body=await req.json()}catch{return NextResponse.json({ok:false,error:'Request body must be valid JSON.'},{status:400})}try{const r=await fetch(`${base}/v1/score`,{method:'POST',headers:learningHeaders(true),body:JSON.stringify(body),cache:'no-store',signal:AbortSignal.timeout(20_000)});return learningResponse(r)}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:'Learning service unavailable'},{status:502})}}
